#include <stdio.h>

int main()
{
int y, x, i;
printf("Digite x: ");
scanf("%d", &x);
printf("Digite y: ");
scanf("%d", &y);

for(i =x; i<=y;i++)
{
	printf("%d\n", i);
}
	
	return 0;
}

